# ============================================================================
# Odiseo MCP Server - Production Dependencies
# ============================================================================
# Generated from pyproject.toml for compatibility
# For package management, use: uv pip install --system -r requirements.txt

# MCP Framework (includes starlette, httpx, pydantic as dependencies)
mcp>=1.23.0

# Database (async)
asyncpg>=0.29.0
pgvector>=0.4.1

# AI/ML - Google Gemini Embeddings
google-genai>=1.49.0

# Google Calendar API (for bookings integration)
google-api-python-client>=2.100.0

# Configuration Management
python-dotenv>=1.0.0
pydantic>=2.11.0
pydantic-settings>=2.11.0

# Web Server
uvicorn>=0.30.0

# ============================================================================
# Development Dependencies (install with: uv pip install --system ruff pytest mypy)
# ============================================================================
# ruff>=0.8.0        # Linting and formatting
# pytest>=8.0.0      # Testing framework
# httpx>=0.27.0      # HTTP client for testing

# ============================================================================
# Installation Instructions (using uv - 10-100x faster than pip)
# ============================================================================
# Install uv: curl -LsSf https://astral.sh/uv/install.sh | sh
# Production: uv pip install --system -r requirements.txt
# Development: uv pip install --system -r requirements.txt ruff pytest mypy
